name: Shell Commands

on: push

jobs:
  run-shell-commands:
    runs-on: ubuntu-20.04
    steps:
      - name: Echo a string
        run: echo "Hello, World!"
      - name: Multiline script
        run: |
          node -v
          npm -v
